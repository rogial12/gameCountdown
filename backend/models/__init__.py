# Este arquivo (mesmo vazio de lógica) marca a pasta 'models' como um PACOTE Python —
# ou seja, uma pasta que pode ser importada com 'from models.schemas import Game'.
# A camada de modelos guarda o schema Pydantic (o contrato de API) e, mais adiante,
# o model SQLAlchemy (o domínio fundido ao ORM). Por ora, só o contrato.
