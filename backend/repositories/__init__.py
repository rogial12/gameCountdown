# Marca a pasta 'repositories' como um PACOTE Python (importável).
# Guarda as INTERFACES de repositório (os contratos das fontes de dados) e, mais adiante,
# suas implementações. Por ora, só a interface GameRepository.
